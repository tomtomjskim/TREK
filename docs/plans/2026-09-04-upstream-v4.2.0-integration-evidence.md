# TREK v4.2.0 Fork Integration Evidence

> 작성일: 2026-09-04
> 갱신일: 2026-09-05
> 상태: fork `main` fast-forward/push 완료; immutable arm64 image 배포 및 production/rollback 검증 완료
> branch: `main` (image source branch: `sync/upstream-v4.2.0`)
> fork checkpoint parent: `09c490e1456908dc2e3cab4a493fe03c669e5dc0`
> official target: annotated tag object `11f390cf1975d896b7b958a8d9c928a37996fbbd`, peeled commit `09ce5cb733bb681c992dfd4f029706718aae58cc`
> verified/deployed code commit: `13c4a137751c53382a58521434657923e34c5080`
> running production: `trek:4.2.0-jsnetworkcorp-13c4a137` (`sha256:8f43b5cfee96ea33ff3e24233f3b83b5d9c80cbead61496b7016a456b1b8ddd3`)
> CI: run `33970857309`; ten core jobs passed, Sonar Scan blocked by missing repository token

This is the evidence ledger for the v4.2.0 integration and authorized production landing. A
command is marked GREEN only after it has run against the merged tree or the named deployed image.

## Decision

- Preserve the reviewed v4.1.1 checkpoint: **GO**
- Integrate exact upstream v4.2.0 in this isolated branch: **GO**
- Treat code commit `13c4a137751c53382a58521434657923e34c5080` as the release source: **GO**
- Merge/push fork `main`: **GO — completed by fast-forward without conflict**
- Production migration/deployment: **GO — completed with verified rollback pair**
- Follow-up runtime hardening/image replacement: **GO — completed; immediate rollback is `e258a7b8`**
- Sonar analysis at `13c4a137`: **BLOCKED — `SONAR_TOKEN` is not configured; not treated as green**

The v4.1.1 tag is an ancestor of v4.2.0. The incremental delta is 87 commits, 605 files and
`+18,505/-2,193`. The initial merge had 42 content conflicts: 23 locale, 5 client and 14 server.
All 42 were resolved without taking either side wholesale, and no conflict marker remains.

## Preserved fork behavior and accepted upstream behavior

| Surface | Integrated contract |
| --- | --- |
| public Trip share | Public place `notes` remain distinct from address, description, assignment/day/collaboration notes; anonymous DTO remains allow-listed and carries no CARTO credential |
| planner | v4.2 multi-reservation rows combine with fork assignment notes, weather/carrier behavior and read-only constraints |
| maps | Authenticated CARTO templates receive a configured key; keyless CARTO and all public links use the tokenless OpenFreeMap fallback; offline prefetch resolves the same effective basemap |
| auth/PWA | Fork language/enrichment/PWA boundaries combine with v4.2 passkey, shutdown and cache behavior |
| Journey share | Shared/public non-skeleton visibility, provider/asset/journey photo binding and empty CARTO compatibility field are preserved; public gallery chronology ignores private/skeleton entry dates |
| Memories | Provider surfaces require the Journey addon; addon-off mutation and album sync now fail before any database write or external provider call |
| packing | Owner/recipient privacy and capability gates combine with all-member bag totals; invalid bag deletion fails without broadcasts; weights are nonnegative integers and quantity is an integer from 1 through 999 at request and service boundaries |
| migrations | Official markers 201–205 run before stable fork migrations; migration 204's default plugin action scope makes a code-only rollback to pre-v4 unsafe once v4.2 data is live |

## Review-driven corrections

Independent conflict reviews initially returned four material findings, all reproduced before the
fixes:

1. `delete_packing_bag` reported success and broadcast for a missing bag.
2. Packing accepted negative/fractional weights and fractional or unbounded quantities.
3. A public Journey photo without `taken_at` could be ordered by a private/skeleton entry date.
4. Some Memories mutations and sync paths bypassed the Journey-addon gate or called the provider
   before rejecting the request.

Regression tests were added first. The fixes are narrow: missing-bag result checks, shared/MCP
numeric schemas plus service normalization, a public-only gallery order fragment, and service-level
addon preflight before mutation/provider access. The private Journey gallery order and legacy
packing response parsing remain unchanged.

The packing REST response now includes server-computed bag and unassigned totals. MCP/plugin list
shapes remain backward-compatible and therefore do not yet expose the unassigned aggregate; a
future additive/versioned contract is preferable to breaking existing plugin consumers.

The follow-up client review added the following focused preservation contracts. Public share map
projection retains valid latitude/longitude `0` values and excludes non-finite or out-of-range
coordinates; place notes remain the shared place note rather than a personal note. CARTO keys are
trimmed before URL binding, and blank/whitespace-only values select the keyless fallback. Audit
detail output redacts secret-like fields. The mobile sheet keeps topmost Escape handling, focus
restoration, and body fixed-scroll locking. On coarse-pointer iPad input, long-press drag remains
available through the touch bridge while native HTML drag is disabled; planner controls expose a
real 44×44 hit target. Vacay deletion is a successful partial operation when the follow-up stats
refresh fails, with the failure observable for diagnostics. Mobile packing uses the same concurrency
and request semantics as desktop. E2E fixtures now enable the required enrichment flags, dismiss
and assert absence of notices before map probes, and clean created state in `finally` blocks so
each test is independently rerunnable.

## Fresh verification ledger

| Gate | Command/scope | Result | State |
| --- | --- | --- | --- |
| merge integrity | unmerged index, marker search, `git diff --check`, cached check | 0 unresolved paths; 0 conflict markers; whitespace checks pass | GREEN |
| official/fork migrations | generated stock/legacy/drift/crash matrix | 23 tests passed; latest official marker asserted as 205 and migrations 201–205 data/defaults verified | GREEN |
| schema migration matrix | migration schema suite | 23 tests passed | GREEN |
| shared packing schema | `packing.schema.spec.ts` | 9 passed | GREEN |
| packing service + MCP | numeric, totals, privacy, missing bag and template-name regressions | 148 passed in final focused run | GREEN |
| Journey share | public chronology/privacy/photo projection plus private-order guard | 41 public-share and 2 private-order tests passed | GREEN |
| Memories addon gate | unified service mutation/sync regressions | 18 passed | GREEN |
| client conflict focus | API, planner, auth, sync/prefetch | 8 files, 574 passed | GREEN |
| public/CARTO/Journey client | share page, settings, tile/basemap, Journey and planner wiring | 9 files, 385 passed | GREEN |
| shared full tests | all shared Vitest files | 45 files, 366 passed | GREEN |
| shared/server/client typecheck | `npm run typecheck --workspaces --if-present` | all available workspace typechecks passed on the final integrated tree | GREEN |
| root build | `npm run build` | shared, server and production client/PWA builds passed; existing chunk-size warning remains | GREEN |
| plugin SDK | isolated dependency install, build, test, coverage | build passed; 19 files passed/1 skipped, 340 tests passed/8 skipped; 51.01% statements | GREEN |
| focused public share coordinates | `SharedTripPage` regression suite | 67 passed; valid zero coordinates retained and invalid coordinates excluded | GREEN |
| focused tile URL contract | `tileUrl.test.ts` | 27 passed; trimmed key and whitespace-only fallback covered | GREEN |
| focused Vacay store | delete/stats partial-success suite | 24 passed | GREEN |
| focused iPad/planner controls | sidebar, touch bridge, TripPlanner suites | 66 + 32 + 61 passed; coarse-pointer drag and 44×44 hit target covered | GREEN |
| focused mobile packing | mobile and desktop packing suites | 36 + 38 passed; request/concurrency parity covered | GREEN |
| matching Playwright evidence | Playwright v1.61.0 image | final scroll-lock tree: URL-bar origin/non-origin 2/2 plus iPad 1/1 passed; responsive five viewports 5/5 and place enrichment 2/2 also passed | GREEN |
| client full tests | frozen two-shard Vitest suite | both final shard commands exited 0; total 653 files, 13,558 passed/38 skipped, 0 failed | GREEN |
| server full tests | complete server Vitest suite, single worker | JSON `success=true`; 10,348 passed/22 pending, 0 failed | GREEN |
| check-only lint | no `--fix`: full client and server plus changed paths | client: 0 errors/1,435 warnings; server: 0 errors/2,548 warnings; changed client/server paths: 0 errors | GREEN WITH BASELINE WARNINGS |
| format compatibility | root/changed-path check-only Prettier | root gate stops at 160 pre-existing shared files; current v4.2 source style also conflicts with configured semicolon rules in 34 changed client and 3 changed server files, so no conflict-amplifying mass rewrite was applied; `git diff --check` passes | RECORDED BASELINE |
| server E2E | E2E subset from the final complete server run | 38 files, 488 passed, 0 failed | GREEN |
| public/app Playwright | final focused Chromium setup/public/app projects | 7/7 passed: public place notes, private-note exclusion, mobile overflow, OFM Korean labels and keyless CARTO fallback without CARTO requests | GREEN |
| npm production audit | `npm audit --omit=dev --json` | high 0, critical 0, moderate 3, low 1; fixes available, no auto-fix applied | RECORDED RISK |
| adversarial/closeout review | clean code commit `e258a7b8`; current full-suite, browser, typecheck and build evidence | two independent final reviews reported blocker 0 and GO | GREEN |
| initial immutable image | native Docker build with `APP_VERSION` and OCI revision/version labels | linux/arm64 image `sha256:29248063...bdbc8f1`; labels and exact app version match `e258a7b8` | GREEN |
| initial clean-install image smoke | read-only root filesystem/network-none container; start, restart and non-mutating SQLite integrity probes | `e258a7b8` healthy twice; version exact; schema 205, fork migrations 3, quick check `ok`, 0 FK findings | GREEN |
| production-copy migration rehearsal | stopped-point snapshot extracted to an isolated strict container | schema 175 → 205; fork ledger 3 → 4 (new auth-session revocation migration); business row counts unchanged; healthy twice; quick check `ok`, 0 FK findings | GREEN |
| old-image rollback rehearsal | same snapshot extracted separately and booted with prior immutable image | v3.4.1 healthy; schema 175/fork 3 retained; business row counts unchanged; quick check `ok`, 0 FK findings | GREEN |
| production cutover | compose image-only change and post-start checks | public/local healthy; HTTPS redirect; version exact; restart 0; image, mounts, IP, port, read-only root and capabilities match baseline; schema 205/fork 4; business row counts unchanged | GREEN |
| rollback artifacts | stopped-point `data` and `uploads` archives plus checksum file | `/mnt/oci-block-volume/ops/trek-v42-cutover-20260905T124004Z`; both SHA-256 and tar structure checks pass; mode 0600 in mode-0700 directory | GREEN |
| follow-up main CI | GitHub Actions run `33970857309` at `13c4a137` | types/lint/facts/i18n plus S3, SDK, shared, client and server coverage all passed; Sonar Scan alone failed because repository `SONAR_TOKEN` is not configured | CORE GREEN / EXTERNAL CONFIG BLOCKED |
| follow-up immutable image | native ARM64 build and disposable read-only/network-none smoke | `sha256:8f43b5cf...8ddd3`; revision/version exact, healthy, restart 0 | GREEN |
| follow-up code-only cutover | canonical TREK Compose project and non-mutating post-start DB probes | `trek`, correct Compose files, read-only root, healthy/restart 0; schema 205/fork 4, quick check `ok`, 0 FK findings | GREEN |

## Post-rebuild validation

The running `main` branch was rebuilt once more after the packing quantity save regression was
contained and the Sonar release version was aligned. The follow-up image is
`trek:4.2.0-jsnetworkcorp-13c4a137` and the live container reports `APP_VERSION`
`4.2.0+jsnetworkcorp.13c4a137`.

- `client/src/components/Packing/PackingListPanelItemRow.test.tsx`,
  `client/src/components/Packing/PackingListPanel.test.tsx` and
  `client/src/store/slices/packingSlice.test.ts` passed together: 169 tests, 3 files, no
  unhandled rejection.
- GitHub Actions run `33970857309` passed all ten core jobs. Client coverage finished with
  653 files, 13,559 passed/38 skipped and 94.24/87.31/93.53/96.01 coverage; server coverage
  finished with 502 files passed/1 skipped, 10,368 passed/22 skipped and
  91.33/83.34/93.13/93.91 coverage. The final Scan job failed independently because this
  repository has no configured Actions secrets and `SONAR_TOKEN` was empty; the workflow remains
  fail-closed instead of turning that missing analysis into a green result.
- The rebuilt image `trek:4.2.0-jsnetworkcorp-13c4a137` came up healthy with restart count 0.
  A parallel QA task initially used the workspace-root Compose project, which started a temporary
  container without the required read-only root filesystem. It was detected, stopped and removed;
  the canonical TREK Compose project was then recreated with the same image and retained data
  mounts. The current container is named `trek`, uses the expected two project Compose files and
  reports `read_only=true`.
- `CARTO_API_KEY` remains empty in the running container, so Plan map rendering still uses the
  keyless OpenFreeMap fallback.
- `curl http://127.0.0.1:3000/api/health` returns `{"status":"ok"}`.

Test runs emit existing mocked-error, React `act(...)`, MSW unhandled-request and build chunk-size
warnings. They are recorded as warnings and are not silently treated as failures. Earlier
monolithic client/server coverage attempts ended with session `SIGTERM(143)` and are not treated
as green coverage evidence; complete test suites were captured through client sharding and the
server full run.

## Optional Sonar provider follow-up

Run `33970857309` remains historical evidence for the pre-detachment workflow: all ten core jobs
passed and the final Scan failed because this fork had no `SONAR_TOKEN`. The optional-provider
branch changes the CI boundary without changing the application, database or deployment:

- coverage jobs and the scan stay in one workflow, so an enabled scan reuses artifacts from the
  same run instead of duplicating tests or trying to consume another run's artifacts;
- `SONAR_ENABLED` absent or not exactly `true` skips only `Optional Sonar Scan`; the ten core jobs
  remain independent of a Sonar account;
- when enabled, `SONAR_TOKEN`, `SONAR_PROJECT_KEY` and `SONAR_ORGANIZATION` must all be non-empty;
  a preflight reports missing setting names without printing values and stops before the scanner;
- the checked-in upstream Sonar project identity and badge are removed. Fork project identity is
  injected only through repository variables;
- detachment is a repository-setting rollback: set `SONAR_ENABLED=false` (or remove it), then
  optionally remove the provider vars and secret. No image rebuild, schema rollback or production
  restart is involved.

The static contract is `node --test scripts/ci-workflow-contract.test.mjs`. At this document
checkpoint the isolated branch has not yet been pushed or exercised by GitHub Actions, so the new
workflow state is **PENDING REMOTE VALIDATION**, not recorded as green.

## CARTO production diagnosis

The deployed v4.2.0 container and migrated database were queried without mutation or returning any
credential value. The Compose `read_only` claim refers to the container root filesystem, not to the
application SQLite handle; TREK requires normal database writes at runtime:

- `settings` contains zero `carto_api_key` rows;
- `app_settings` contains zero `default_user_setting_carto_api_key` rows;
- the container has no non-empty `CARTO_API_KEY` environment variable;
- no user or instance-default map template points at `basemaps.cartocdn.com`.

Therefore the prior plan-tab watermark was not evidence of an expired bound key. No CARTO key was
bound to TREK at cutover time; CARTO account registration and saving the issued key into TREK are
separate steps. v4.2.0 now selects OpenFreeMap when no key is present, including the Plan map. If
CARTO is deliberately selected later, the key must be saved as a user or instance default and
validated against CARTO. The key itself was neither printed nor written to this ledger.

## Rollback status and residual risks

After a v4.2 database migration, rollback must pair the previous image with the same-logical-point
pre-v4.2 database and uploads snapshot. A code-only rollback is unsafe because the new
`plugin_actions.scope` default can make instance actions visible as user actions to older code.
This deployment retains that exact pair and rehearsed it: image
`sha256:97a4312f262b53c36bc3364ba62ae84282782003db7b68b59ebc60f37c02bd07` plus the archives named
above. The instance had zero installed plugins and no external plugin/storage state at cutover.
The follow-up `13c4a137` image did not change schema or fork migration state, so its immediate
rollback is the still-retained `e258a7b8` image without restoring the pre-v4.2 snapshot. The
v3.4.1 image must only be paired with that stopped-point snapshot.

Remaining non-blocking risks:

- Both the prior and new image required Docker's timeout kill during rehearsal/stop (exit 137,
  `OOMKilled=false`). Stopped-point and post-kill SQLite checks were `ok`, but graceful container
  shutdown latency should be diagnosed separately.
- No authenticated production browser session was used for the Plan tab. The exact deployed source
  passed isolated authenticated Playwright coverage, and public/local app version binding confirms
  the running image. On 2026-09-07, password registration was still disabled, so no production test
  account or trip was created. A fresh localhost-only container from the exact deployed immutable
  image completed authentication, trip/place creation and rendered both the Leaflet shell and
  MapLibre canvas. That follow-up did not complete a new request-host/screenshot gate because the
  production-build `window.__trek_map.loaded()` wait timed out; it does not replace or weaken the
  earlier 7/7 request-based Playwright evidence.
- Host WebKit remains limited by the missing `libwoff2dec.so.1.0.2`; Chromium and the earlier
  matching-image WebKit replacement evidence are retained.
- Production audit remains high 0/critical 0/moderate 3/low 1; no dependency auto-fix was mixed
  into the release.
- GitHub Actions has no repository `SONAR_TOKEN`, and the 2026-09-07 host environment also had no
  local token to register; run `33970857309` therefore retains a red Scan conclusion even though
  all ten code/test/coverage jobs passed. Configure a valid `liketrek_TREK` project token before
  treating Sonar analysis as current or rerunning the failed job.
- The configured shared Prettier gate retains its pre-existing baseline failures; no mass formatting
  rewrite was mixed into the upstream merge.
- Upstream's mutated `plugin-sdk-v1.7.0` tag and unchanged SDK package version remain upstream
  provenance concerns, not blockers for the deployed core image.
