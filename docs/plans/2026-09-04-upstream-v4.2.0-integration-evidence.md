# TREK v4.2.0 Fork Integration Evidence

> 작성일: 2026-09-04
> 갱신일: 2026-09-05
> 상태: isolated candidate integrated; test/build/typecheck/WebKit and independent closeout evidence captured; main/push/build-image/deploy 없음
> branch: `sync/upstream-v4.2.0`
> fork checkpoint parent: `09c490e1456908dc2e3cab4a493fe03c669e5dc0`
> official target: annotated tag object `11f390cf1975d896b7b958a8d9c928a37996fbbd`, peeled commit `09ce5cb733bb681c992dfd4f029706718aae58cc`
> verified code commit: `720920f1c2ebe41ceba3633509c7cf3a75685939`
> running production: `trek:3.4.1-jsnetworkcorp-7a50356e` (unchanged)

This is the evidence ledger for the isolated v4.2.0 integration candidate. A command is marked
GREEN only after it has run against the merged tree. It does not authorize a fork-main merge,
remote push, image build, database migration or deployment.

## Decision

- Preserve the reviewed v4.1.1 checkpoint: **GO**
- Integrate exact upstream v4.2.0 in this isolated branch: **GO**
- Treat code commit `720920f1c2ebe41ceba3633509c7cf3a75685939` as a locally testable
  candidate: **GO from code/test evidence**
- Merge/push fork `main`: **NO-GO** without a separate release decision
- Production migration/deployment: **NO-GO**

The v4.1.1 tag is an ancestor of v4.2.0. The incremental delta is 87 commits, 605 files and
`+18,505/-2,193`. The initial merge had 42 content conflicts: 23 locale, 5 client and 14 server.
All 42 were resolved without taking either side wholesale, and no conflict marker remains.

## Preserved fork behavior and accepted upstream behavior

| Surface | Integrated contract |
| --- | --- |
| public Trip share | Public place `notes` remain distinct from address, description, assignment/day/collaboration notes; anonymous DTO remains allow-listed and carries no CARTO credential |
| planner | v4.2 multi-reservation rows combine with fork assignment notes, weather/carrier behavior and read-only constraints |
| maps | Authenticated CARTO templates receive a configured key; keyless CARTO and all public links use the tokenless OpenFreeMap fallback; offline prefetch resolves the same effective basemap |
| auth/PWA | Fork language/enrichment/PWA boundaries combine with v4.2 passkey, shutdown and cache behavior |
| Journey share | Shared/public non-skeleton visibility, provider/asset/journey photo binding and empty CARTO compatibility field are preserved; public gallery chronology ignores private/skeleton entry dates |
| Memories | Provider surfaces require the Journey addon; addon-off mutation and album sync now fail before any database write or external provider call |
| packing | Owner/recipient privacy and capability gates combine with all-member bag totals; invalid bag deletion fails without broadcasts; weights are nonnegative integers and quantity is an integer from 1 through 999 at request and service boundaries |
| migrations | Official markers 201–205 run before stable fork migrations; migration 204's default plugin action scope makes code-only rollback unsafe once v4.2 data is live |

## Review-driven corrections

Independent conflict reviews initially returned four material findings, all reproduced before the
fixes:

1. `delete_packing_bag` reported success and broadcast for a missing bag.
2. Packing accepted negative/fractional weights and fractional or unbounded quantities.
3. A public Journey photo without `taken_at` could be ordered by a private/skeleton entry date.
4. Some Memories mutations and sync paths bypassed the Journey-addon gate or called the provider
   before rejecting the request.

Regression tests were added first. The fixes are narrow: missing-bag result checks, shared/MCP
numeric schemas plus service normalization, a public-only gallery order fragment, and service-level
addon preflight before mutation/provider access. The private Journey gallery order and legacy
packing response parsing remain unchanged.

The packing REST response now includes server-computed bag and unassigned totals. MCP/plugin list
shapes remain backward-compatible and therefore do not yet expose the unassigned aggregate; a
future additive/versioned contract is preferable to breaking existing plugin consumers.

The follow-up client review added the following focused preservation contracts. Public share map
projection retains valid latitude/longitude `0` values and excludes non-finite or out-of-range
coordinates; place notes remain the shared place note rather than a personal note. CARTO keys are
trimmed before URL binding, and blank/whitespace-only values select the keyless fallback. Audit
detail output redacts secret-like fields. The mobile sheet keeps topmost Escape handling, focus
restoration, and body fixed-scroll locking. On coarse-pointer iPad input, long-press drag remains
available through the touch bridge while native HTML drag is disabled; planner controls expose a
real 44×44 hit target. Vacay deletion is a successful partial operation when the follow-up stats
refresh fails, with the failure observable for diagnostics. Mobile packing uses the same concurrency
and request semantics as desktop. E2E fixtures now enable the required enrichment flags, dismiss
and assert absence of notices before map probes, and clean created state in `finally` blocks so
each test is independently rerunnable.

## Fresh verification ledger

| Gate | Command/scope | Result | State |
| --- | --- | --- | --- |
| merge integrity | unmerged index, marker search, `git diff --check`, cached check | 0 unresolved paths; 0 conflict markers; whitespace checks pass | GREEN |
| official/fork migrations | generated stock/legacy/drift/crash matrix | 23 tests passed; latest official marker asserted as 205 and migrations 201–205 data/defaults verified | GREEN |
| schema migration matrix | migration schema suite | 23 tests passed | GREEN |
| shared packing schema | `packing.schema.spec.ts` | 9 passed | GREEN |
| packing service + MCP | numeric, totals, privacy, missing bag and template-name regressions | 148 passed in final focused run | GREEN |
| Journey share | public chronology/privacy/photo projection plus private-order guard | 41 public-share and 2 private-order tests passed | GREEN |
| Memories addon gate | unified service mutation/sync regressions | 18 passed | GREEN |
| client conflict focus | API, planner, auth, sync/prefetch | 8 files, 574 passed | GREEN |
| public/CARTO/Journey client | share page, settings, tile/basemap, Journey and planner wiring | 9 files, 385 passed | GREEN |
| shared full tests | all shared Vitest files | 45 files, 366 passed | GREEN |
| shared/server/client typecheck | `npm run typecheck --workspaces --if-present` | all available workspace typechecks passed on the final integrated tree | GREEN |
| root build | `npm run build` | shared, server and production client/PWA builds passed; existing chunk-size warning remains | GREEN |
| plugin SDK | isolated dependency install, build, test, coverage | build passed; 19 files passed/1 skipped, 340 tests passed/8 skipped; 51.01% statements | GREEN |
| focused public share coordinates | `SharedTripPage` regression suite | 67 passed; valid zero coordinates retained and invalid coordinates excluded | GREEN |
| focused tile URL contract | `tileUrl.test.ts` | 27 passed; trimmed key and whitespace-only fallback covered | GREEN |
| focused Vacay store | delete/stats partial-success suite | 24 passed | GREEN |
| focused iPad/planner controls | sidebar, touch bridge, TripPlanner suites | 66 + 32 + 61 passed; coarse-pointer drag and 44×44 hit target covered | GREEN |
| focused mobile packing | mobile and desktop packing suites | 36 + 38 passed; request/concurrency parity covered | GREEN |
| matching Playwright evidence | Playwright v1.61.0 image | final scroll-lock tree: URL-bar origin/non-origin 2/2 plus iPad 1/1 passed; responsive five viewports 5/5 and place enrichment 2/2 also passed | GREEN |
| client full tests | frozen two-shard Vitest suite | shard 1: 326 files, 7,319 passed/38 skipped, 1,696.15s; shard 2: 325 files, 6,181 passed, 1,494.95s; total 651 files, 13,500 passed/38 skipped | GREEN |
| server full tests | complete server Vitest suite | 496 files passed/1 skipped; 10,223 passed/22 skipped | GREEN |
| check-only lint | no `--fix`: full client and server plus changed paths | client: 0 errors/1,437 warnings; server: 0 errors/328 warnings; changed client/server paths: 0 errors | GREEN WITH BASELINE WARNINGS |
| format compatibility | root/changed-path check-only Prettier | root gate stops at 160 pre-existing shared files; current v4.2 source style also conflicts with configured semicolon rules in 34 changed client and 3 changed server files, so no conflict-amplifying mass rewrite was applied; `git diff --check` passes | RECORDED BASELINE |
| server E2E | complete E2E workspace suite | 38 files, 484 passed | GREEN |
| public/app Playwright | normal public and authenticated projects | host Chromium auth+public screenshot 5/5; 3 host WebKit launches fail because `libwoff2dec.so.1.0.2` is absent, while the matching-image final WebKit replacement gate passed 3/3 | GREEN WITH HOST LIMITATION |
| npm production audit | `npm audit --omit=dev --json` | high 0, critical 0, moderate 3; fixes available, no auto-fix applied | RECORDED RISK |
| adversarial/closeout review | code commit `720920f1`; committed paths, fresh screenshots, temp cleanup and residual risks | independent reviewer reported no HIGH/MED/LOW findings; closeout evidence is current and bound to the committed tree | GREEN |

Test runs emit existing mocked-error, React `act(...)`, MSW unhandled-request and build chunk-size
warnings. They are recorded as warnings and are not silently treated as failures. Earlier
monolithic client/server coverage attempts ended with session `SIGTERM(143)` and are not treated
as green coverage evidence; complete test suites were captured through client sharding and the
server full run.

## CARTO production diagnosis

The running v3.4.1 container was checked read-only without returning any credential value:

- `settings` contains zero `carto_api_key` rows;
- `app_settings` contains zero `default_user_setting_carto_api_key` rows;
- the container has no non-empty `CARTO_API_KEY` environment variable;
- the container is not in managed mode.

Therefore the current plan-tab watermark is not evidence of an expired bound key. No CARTO key is
bound to the running application at all. Registering with CARTO and saving the issued key into TREK
are separate steps. The candidate removes the watermark when no key is present by selecting the
OpenFreeMap fallback; if CARTO is deliberately selected, the key must still be saved as a user or
instance default and validated against CARTO after deployment. The key itself was neither printed
nor written to this ledger.

## Rollback and release blockers

After a v4.2 database migration, rollback must pair the previous image with the same-logical-point
pre-v4.2 database, uploads and plugin-state snapshot. A code-only rollback is unsafe because the
new `plugin_actions.scope` default can make instance actions visible as user actions to older code.

The following remain outside this isolated code integration and keep deployment at NO-GO:

- process-wide write quiescing and crash journal/recovery for restore;
- backup encryption-key identity/fingerprint proof;
- sanitized full-state restore rehearsal with checksum-bound DB/uploads/plugin state;
- immutable image digest and signing/provenance evidence;
- Android/device-browser evidence and production rollback rehearsal;
- upstream's mutated `plugin-sdk-v1.7.0` tag and unchanged SDK package version despite source changes.
